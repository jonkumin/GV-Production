"use strict";

var _path = require("path");

var _resolvePackagePath = _interopRequireDefault(require("resolve-package-path"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * During build initialization we need to know core XDN version
 * before any of the build is actually done.
 *
 * Notes:
 * - CLI may be global but not local.
 * - CLI must not depend on @xdn/core to avoid long installation times.
 */
module.exports = function getCoreXdnVersion() {
  _resolvePackagePath.default._resetCache();

  const path = (0, _resolvePackagePath.default)('@xdn/core', process.cwd());

  if (!path) {
    return null;
  }

  const {
    DeploymentBuilder
  } = require((0, _path.join)((0, _path.dirname)(path), 'deploy'));

  return DeploymentBuilder.xdnVersion;
};