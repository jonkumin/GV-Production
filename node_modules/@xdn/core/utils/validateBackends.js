"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var os_1 = require("os");
var constants_1 = require("../constants");
var config_1 = __importDefault(require("../config"));
/**
 * Validates if provided backend exists in the config
 * @param backend
 */
function validateBackends(backend) {
    if (Object.values(constants_1.BACKENDS).includes(backend)) {
        return;
    }
    if (config_1.default.get("backends." + backend, false)) {
        return;
    }
    var backends = Object.keys(config_1.default.get('backends'));
    if (backends && backends.length) {
        throw new Error("To fix this problem use one of the \"backends\" configured in xdn.config.js:" + os_1.EOL + os_1.EOL +
            (" - " + backends.join(os_1.EOL + " - ") + os_1.EOL + os_1.EOL) +
            ("or add \"" + backend + "\" to the \"backends\" property in xdn.config.js."));
    }
    else {
        throw new Error("Add \"" + backend + "\" to the \"backends\" property in xdn.config.js.");
    }
}
exports.default = validateBackends;
