import Timing from './Timing';
export default class RequestContext {
    /**
     * Values for x-xdn-t
     */
    timing: Timing;
    /**
     * Set to true if ResponseWriter#cache is called with edge: { forcePrivateCaching: true }
     */
    forcePrivateCaching: boolean;
    /**
     * The method criteria from the route that set caching
     */
    cacheRouteMethod?: string;
    cacheKey?: string;
}
