import { CacheKeyWillBeUsedCallbackParam, WorkboxPlugin } from 'workbox-core/types';
import { CustomCacheKeyConfig } from '@xdn/core/router/CustomCacheKey';
export default class CustomCacheKeyPlugin implements WorkboxPlugin {
    route: RegExp;
    config: CustomCacheKeyConfig[];
    constructor(urls: RegExp, config: CustomCacheKeyConfig[]);
    urlMatches(url: string): boolean;
    cacheKeyWillBeUsed({ request }: CacheKeyWillBeUsedCallbackParam): Promise<string | Request>;
    private handleKeyConfig;
}
